{
  "enabled": true,
  "description": "Nightly ETL: pulls order data from Postgres, transforms, loads into the warehouse",
  "tags": ["etl", "scheduled", "warehouse"],
  "timeout": 900,
  "retry_attempts": 2,
  "schedule_cron": "0 3 * * *",
  "cpu_hard_limit": 2,
  "mem_hard_limit": "1G"
}
