# Analyse-Scope fuer SonarQube Cloud (Automatic Analysis).
#
# website/support.js ist ein generiertes, eingecheckt-vendortes Fremd-Bundle
# ("dc-runtime", Design-Canvas-Runtime). Der Datei-Header sagt:
#   "GENERATED from dc-runtime/src/*.ts - do not edit."
# Das TypeScript-Quellverzeichnis dc-runtime/ liegt NICHT in diesem Repo. Findings
# im Bundle sind hier also nicht behebbar, und jede Handaenderung waere beim
# naechsten Vendor-Drop wieder weg.
#
# Konkret Anlass: jssecurity:S8476 ("Client-Side Request Forgery via unsanitized
# user input") auf dem fetch(location.href) in boot(). Der Call holt das EIGENE
# Dokument erneut, um das rohe <x-dc>-Template zu bekommen, nachdem die DOM-Kopie
# bereits geparst wurde. location.href ist per Definition die URL, aus der das
# Dokument geladen wurde -> der Request ist konstruktionsbedingt same-origin.
# Ein Angreifer kann ihn weder auf einen fremden Host umlenken noch zu einem
# state-changing Request machen; die Antwort sind die eigenen Bytes der Seite.
# Bewertet als False Positive, Details in website/README.md.
sonar.exclusions=website/support.js
