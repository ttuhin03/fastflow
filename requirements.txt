# Fast-Flow Orchestrator – Production-ready pins (compatible release)
# Install: pip install -r requirements.txt

fastapi>=0.141.1,<1
prometheus-fastapi-instrumentator>=8.1.0,<9
uvicorn[standard]>=0.52.1,<1
sqlmodel>=0.0.39,<0.1
docker>=7.2.0,<8
kubernetes>=36.0.3,<37
apscheduler>=3.11.3,<4
pydantic>=2.13.4,<3
email-validator>=2.3.0,<3
python-multipart>=0.0.32,<1
aiofiles>=25.1.0,<26
alembic>=1.19.0,<2
boto3>=1.43.66,<2
python-dotenv>=1.2.2,<2
python-jose[cryptography]>=3.5.0,<4
cryptography>=50.0.0
PyJWT>=2.13.0,<3
requests>=2.34.2,<3
httpx>=0.28.1,<0.29
aiosmtplib>=5.1.2,<6
psycopg2-binary>=2.9.12,<3
psutil>=7.2.2,<8
pytest>=9.1.1,<10
pytest-asyncio>=1.4.0,<1.5
pytest-cov>=7.1.0,<8
slowapi>=0.1.10,<1
pip-audit>=2.10.1,<3
tenacity>=9.1.4,<10

# ---------------------------------------------------------------------------
# Security-Floors für transitiv/aus dem Base-Image gezogene Pakete.
# Nicht direkt importiert – hier nur gepinnt, damit Trivy/pip-audit keine
# verwundbaren Versionen im Image finden.
# ---------------------------------------------------------------------------

# setuptools: CVE-2025-47273 (Path Traversal in PackageIndex, fixed 78.1.1).
# Floor bleibt bei 80.9.0 wegen vendored jaraco.context CVE-2026-23949.
# Quelle ist das python:3.11-slim Base-Image (Trivy meldete dort 70.3.0).
setuptools>=83.0.0

# msgpack: GHSA-6v7p-g79w-8964 (Out-of-bounds Read / Crash bei Unpacker-Reuse
# nach abgefangenem Fehler, fixed 1.2.1). Transitiv via pip-audit -> CacheControl.
msgpack>=1.2.1
