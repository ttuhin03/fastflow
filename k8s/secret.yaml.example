# Secret-Template für sensible Umgebungsvariablen
#
# Erstellen aus .env:
#   kubectl create secret generic fastflow-secrets \
#     --from-env-file=.env \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# Oder manuell mit einzelnen Keys:
#   kubectl create secret generic fastflow-secrets \
#     --from-literal=ENCRYPTION_KEY="dein-fernet-key" \
#     --from-literal=JWT_SECRET_KEY="mind-32-zeichen-sicherer-key" \
#     --from-literal=GITHUB_CLIENT_ID="..." \
#     --from-literal=GITHUB_CLIENT_SECRET="..."
#
# Pflichtfelder: ENCRYPTION_KEY, JWT_SECRET_KEY
# OAuth: Mind. ein Provider (GITHUB_*, GOOGLE_*, MICROSOFT_*, CUSTOM_OAUTH_*)
#
# Optional für SKIP_OAUTH_VERIFICATION (Tests/CI):
#   --from-literal=SKIP_OAUTH_VERIFICATION=true
