diff --git a/node_modules/@docusaurus/plugin-content-blog/lib/client/sidebarUtils.js b/node_modules/@docusaurus/plugin-content-blog/lib/client/sidebarUtils.js
index 87bd2dd..ab3d1a0 100644
--- a/node_modules/@docusaurus/plugin-content-blog/lib/client/sidebarUtils.js
+++ b/node_modules/@docusaurus/plugin-content-blog/lib/client/sidebarUtils.js
@@ -24,8 +24,10 @@ export function useVisibleBlogSidebarItems(items) {
     return useMemo(() => items.filter((item) => isVisible(item, pathname)), [items, pathname]);
 }
 export function groupBlogSidebarItemsByYear(items) {
-    const groupedByYear = groupBy(items, (item) => {
-        return `${new Date(item.date).getFullYear()}`;
+    const safe = (items ?? []).filter((item) => item && item.date != null);
+    const groupedByYear = groupBy(safe, (item) => {
+        const d = item?.date;
+        return d != null ? `${new Date(d).getFullYear()}` : 'unknown';
     });
     // "as" is safe here
     // see https://github.com/microsoft/TypeScript/pull/56805#issuecomment-2196526425
diff --git a/node_modules/@docusaurus/plugin-content-blog/lib/props.js b/node_modules/@docusaurus/plugin-content-blog/lib/props.js
index c61f3be..5dd8e01 100644
--- a/node_modules/@docusaurus/plugin-content-blog/lib/props.js
+++ b/node_modules/@docusaurus/plugin-content-blog/lib/props.js
@@ -31,13 +31,14 @@ function toAuthorItemProp({ author, count, }) {
     };
 }
 function toBlogSidebarProp({ blogSidebarTitle, blogPosts, }) {
+    const safe = (blogPosts ?? []).filter((p) => p?.metadata && p.metadata.date != null);
     return {
         title: blogSidebarTitle,
-        items: blogPosts.map((blogPost) => ({
-            title: blogPost.metadata.frontMatter.sidebar_label ?? blogPost.metadata.title,
-            permalink: blogPost.metadata.permalink,
-            unlisted: blogPost.metadata.unlisted,
-            date: blogPost.metadata.date,
+        items: safe.map((blogPost) => ({
+            title: blogPost.metadata?.frontMatter?.sidebar_label ?? blogPost.metadata?.title,
+            permalink: blogPost.metadata?.permalink,
+            unlisted: blogPost.metadata?.unlisted,
+            date: blogPost.metadata?.date,
         })),
     };
 }
diff --git a/node_modules/@docusaurus/plugin-content-blog/src/blogUtils.ts b/node_modules/@docusaurus/plugin-content-blog/src/blogUtils.ts
index 3647a34..e758825 100644
--- a/node_modules/@docusaurus/plugin-content-blog/src/blogUtils.ts
+++ b/node_modules/@docusaurus/plugin-content-blog/src/blogUtils.ts
@@ -430,9 +430,11 @@ export async function generateBlogPosts(
     await Promise.all(blogSourceFiles.map(doProcessBlogSourceFile))
   ).filter(Boolean) as BlogPost[];
 
-  blogPosts.sort(
-    (a, b) => b.metadata.date.getTime() - a.metadata.date.getTime(),
-  );
+  blogPosts.sort((a, b) => {
+    const aTime = a?.metadata?.date?.getTime?.() ?? 0;
+    const bTime = b?.metadata?.date?.getTime?.() ?? 0;
+    return bTime - aTime;
+  });
 
   if (options.sortPosts === 'ascending') {
     return blogPosts.reverse();
diff --git a/node_modules/@docusaurus/plugin-content-blog/src/client/sidebarUtils.tsx b/node_modules/@docusaurus/plugin-content-blog/src/client/sidebarUtils.tsx
index 3c276c0..3fe1bff 100644
--- a/node_modules/@docusaurus/plugin-content-blog/src/client/sidebarUtils.tsx
+++ b/node_modules/@docusaurus/plugin-content-blog/src/client/sidebarUtils.tsx
@@ -36,8 +36,10 @@ export function useVisibleBlogSidebarItems(
 export function groupBlogSidebarItemsByYear(
   items: BlogSidebarItem[],
 ): [string, BlogSidebarItem[]][] {
-  const groupedByYear = groupBy(items, (item) => {
-    return `${new Date(item.date).getFullYear()}`;
+  const safe = (items ?? []).filter((item) => item && item.date != null);
+  const groupedByYear = groupBy(safe, (item) => {
+    const d = item?.date;
+    return d != null ? `${new Date(d).getFullYear()}` : 'unknown';
   });
   // "as" is safe here
   // see https://github.com/microsoft/TypeScript/pull/56805#issuecomment-2196526425
diff --git a/node_modules/@docusaurus/plugin-content-blog/src/props.ts b/node_modules/@docusaurus/plugin-content-blog/src/props.ts
index b8fac4d..344d80d 100644
--- a/node_modules/@docusaurus/plugin-content-blog/src/props.ts
+++ b/node_modules/@docusaurus/plugin-content-blog/src/props.ts
@@ -62,9 +62,12 @@ export function toBlogSidebarProp({
   blogSidebarTitle: string;
   blogPosts: BlogPost[];
 }): BlogSidebar {
+  const safe = (blogPosts ?? []).filter(
+    (p) => p?.metadata && p.metadata.date != null,
+  );
   return {
     title: blogSidebarTitle,
-    items: blogPosts.map((blogPost) => ({
+    items: safe.map((blogPost) => ({
       title:
         blogPost.metadata.frontMatter.sidebar_label ?? blogPost.metadata.title,
       permalink: blogPost.metadata.permalink,
